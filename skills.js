function calculateNumbers(var1, var2) {
    const sum = var1 + var2;
    const diff = var1 - var2;
    const mult = var1 * var2;
    const div = var1 / var2;
    const result = `Sum: ${sum}, Diff: ${diff}, Mult: ${mult}, Div: ${div}`;
    return result;
}